import express, { Request, Response } from 'express';
import { body, param } from 'express-validator';
import { authenticate } from '../middleware/auth.middleware';
import { validate } from '../middleware/validation.middleware';

const router = express.Router();

/**
 * @swagger
 * components:
 *   schemas:
 *     ProductType:
 *       type: object
 *       required:
 *         - name
 *       properties:
 *         id:
 *           type: integer
 *           description: The product type ID
 *         name:
 *           type: string
 *           description: The product type name
 *         company_id:
 *           type: integer
 *           description: The company ID this product type belongs to
 *         created_at:
 *           type: string
 *           format: date-time
 *           description: Creation timestamp
 *         updated_at:
 *           type: string
 *           format: date-time
 *           description: Last update timestamp
 */

/**
 * @swagger
 * tags:
 *   name: ProductTypes
 *   description: Product type management
 */

/**
 * @swagger
 * /api/product-types:
 *   get:
 *     summary: Get all product types
 *     tags: [ProductTypes]
 *     security:
 *       - bearerAuth: []
 *     responses:
 *       200:
 *         description: List of product types
 *         content:
 *           application/json:
 *             schema:
 *               type: array
 *               items:
 *                 $ref: '#/components/schemas/ProductType'
 */
router.get('/', authenticate, (req: Request, res: Response): void => {
  // This is a placeholder - implement actual controller logic
  res.status(200).json([]);
});

/**
 * @swagger
 * /api/product-types/{id}:
 *   get:
 *     summary: Get product type by ID
 *     tags: [ProductTypes]
 *     security:
 *       - bearerAuth: []
 *     parameters:
 *       - in: path
 *         name: id
 *         schema:
 *           type: integer
 *         required: true
 *         description: Product type ID
 *     responses:
 *       200:
 *         description: Product type
 *         content:
 *           application/json:
 *             schema:
 *               $ref: '#/components/schemas/ProductType'
 *       404:
 *         description: Product type not found
 */
router.get(
  '/:id',
  authenticate,
  validate([param('id').isInt().withMessage('ID must be an integer')]),
  (req: Request, res: Response): void => {
    // This is a placeholder - implement actual controller logic
    res.status(404).json({ message: 'Product type not found' });
  }
);

/**
 * @swagger
 * /api/product-types:
 *   post:
 *     summary: Create a new product type
 *     tags: [ProductTypes]
 *     security:
 *       - bearerAuth: []
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             $ref: '#/components/schemas/ProductType'
 *     responses:
 *       201:
 *         description: Product type created successfully
 *         content:
 *           application/json:
 *             schema:
 *               $ref: '#/components/schemas/ProductType'
 */
router.post(
  '/',
  authenticate,
  validate([
    body('name').notEmpty().withMessage('Name is required'),
    body('product_group_id').isInt().withMessage('Product Group ID must be an integer'),
    body('company_id').optional().isInt().withMessage('Company ID must be an integer')
  ]),
  (req: Request, res: Response): void => {
    // This is a placeholder - implement actual controller logic
    res.status(201).json({ id: 1, ...req.body });
  }
);

/**
 * @swagger
 * /api/product-types/update:
 *   put:
 *     summary: Update product type
 *     tags: [ProductTypes]
 *     security:
 *       - bearerAuth: []
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             $ref: '#/components/schemas/ProductType'
 *     responses:
 *       200:
 *         description: Product type updated
 *         content:
 *           application/json:
 *             schema:
 *               $ref: '#/components/schemas/ProductType'
 *       400:
 *         description: Invalid request
 *       404:
 *         description: Product type not found
 */
router.put(
  '/update',
  authenticate,
  validate([
    body('id').isInt().withMessage('ID is required and must be an integer'),
    body('name').notEmpty().withMessage('Name is required'),
    body('product_group_id').isInt().withMessage('Product Group ID must be an integer'),
    body('company_id').optional().isInt().withMessage('Company ID must be an integer')
  ]),
  (req: Request, res: Response): void => {
    // This is a placeholder - implement actual controller logic
    res.status(200).json(req.body);
  }
);

/**
 * @swagger
 * /api/product-types/remove/{id}:
 *   delete:
 *     summary: Delete a product type (soft delete)
 *     tags: [ProductTypes]
 *     security:
 *       - bearerAuth: []
 *     parameters:
 *       - in: path
 *         name: id
 *         schema:
 *           type: integer
 *         required: true
 *         description: Product type ID
 *     responses:
 *       200:
 *         description: Product type deleted
 *       404:
 *         description: Product type not found
 *       400:
 *         description: Cannot delete product type that is in use
 */
router.delete(
  '/remove/:id',
  authenticate,
  validate([param('id').isInt().withMessage('ID must be an integer')]),
  (req: Request, res: Response): void => {
    // This is a placeholder - implement actual controller logic
    res.status(200).json({ message: 'Product type deleted successfully' });
  }
);

/**
 * @swagger
 * /api/product-types/product-group/{productGroupId}:
 *   get:
 *     summary: Get product types by product group ID
 *     tags: [ProductTypes]
 *     security:
 *       - bearerAuth: []
 *     parameters:
 *       - in: path
 *         name: productGroupId
 *         schema:
 *           type: integer
 *         required: true
 *         description: Product group ID
 *     responses:
 *       200:
 *         description: List of product types
 *         content:
 *           application/json:
 *             schema:
 *               type: array
 *               items:
 *                 $ref: '#/components/schemas/ProductType'
 */
router.get(
  '/product-group/:productGroupId',
  authenticate,
  validate([param('productGroupId').isInt().withMessage('Product Group ID must be an integer')]),
  (req: Request, res: Response): void => {
    // This is a placeholder - implement actual controller logic
    res.status(200).json([]);
  }
);

export default router;
