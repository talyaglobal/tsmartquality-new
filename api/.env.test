# Test Environment Configuration
NODE_ENV=test

# Database Configuration (using local PostgreSQL for testing)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tsmartquality_test
DB_USER=postgres
DB_PASSWORD=password

# Database Pool Settings
DB_POOL_MAX=5
DB_POOL_MIN=1
DB_IDLE_TIMEOUT=1000
DB_CONNECT_TIMEOUT=3000

# Security
JWT_SECRET=test_jwt_secret_key_for_testing_purposes_only

# Features
ENABLE_QUERY_LOGGING=false
ENABLE_AUTO_MIGRATIONS=true